specVersion: "v1"
serviceId: "pi"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "pi-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
